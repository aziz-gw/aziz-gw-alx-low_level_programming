#include <stdio.h>

/**
 * main - entry point
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: always 0
 */

int main(__attribute__((unused)) int argc, char *argv[])
{

	printf("My name is %s", argv[0]);
	printf("\n");
	return (0);
}
