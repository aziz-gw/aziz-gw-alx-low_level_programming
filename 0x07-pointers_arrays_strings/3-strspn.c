#include "main.h"

/**
 * _strspn - gets length of a prefix substring
 *
 *
 */

unsigned int _strspn(char *s, char *accept)
{
	int byte_num, i;

	for (i = 0; s[i]; i++)
	{
		if (s[i] == accept[j])
			byte_num = accept[j];
	}
	return (byte_num);
}
