#include "main.h"

/**
 *  * _isdigit - checks for a digit
 *   * @d: digit to be checked
 *    *
 *     * Return: 1 if c is a digit and 0 otherwise
 *      */

int _isdigit(int d)
{
	        if (d >= 48 && d <= 57)
			        {
					        return (1);
						        }
		        return (0);
}

