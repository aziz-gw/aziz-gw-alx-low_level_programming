#include "main.h"

/**
 * _strpbrk - searches a string for any of a set of bytes
 *
 * Return: pointer to the bytes of s that matches one
 * of the bytes in accept or NULL if no such byte is found
 */

char *_strpbrk(char *s, char *accept)
{

}
